# `<领域名称>` 模块开发模板

> 本模板用于业务阶段的真实模块设计。阶段0不得据此提前创建业务表或 CRUD。
> 代码分层的可运行示例见 `com.srm.workbench` 与 `/api/v1/workbench/baseline`；
> 新模块应复用其依赖方向，而不是复制工作台的连接探测语义。

## 1. 范围与边界

- 业务目标：
- 上游/下游模块：
- 明确不做：

## 2. 用例与角色

| 用例 | 角色 | 前置条件 | 成功结果 | 权限码 |
| --- | --- | --- | --- | --- |

## 3. 领域模型与状态机

- 聚合/实体/值对象：
- 稳定状态 code：
- 状态转换、守卫条件与审计要求：

## 4. API 契约

- Request/Response DTO：
- 幂等、分页、错误码：
- 内部端/供应商端 OpenAPI 分组、兼容性影响与生成客户端：
- 跨模块公开 QueryService/ApplicationService：
- Controller → application query/service → domain repository → infrastructure persistence：

## 5. 数据设计

- 表、字段、唯一约束与索引：
- MyBatis-Plus Entity/Mapper/Repository 适配器与复杂查询 XML：
- Spring Cache 缓存键、容量、TTL、精确/批量失效（如不缓存需说明）：
- 审计、逻辑删除、乐观锁：
- `source_system`、`external_id`、`external_version`、`idempotency_key` 适用性：

## 6. 前端设计

- 菜单/路由/componentKey/permission：
- 列表、详情和隐藏路由：
- 403、空状态和错误处理：

## 7. 测试与验收

- 单元/集成/边界测试：
- 权限正反例：
- 数据迁移与回滚/恢复说明：
- OpenAPI 规范、破坏性变更和前端生成代码检查：
