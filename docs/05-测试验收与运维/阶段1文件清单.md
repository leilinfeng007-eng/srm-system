# 阶段1文件清单

版本：V1.3 | 更新：2026-08-04 | 分支：`agent/configure-tcr-release` | 接管HEAD：`03ac2d1c52048c6f0b69774c252cd7abe1c5ff27`

## 1. 工作树统计

- 本轮重新接管时实际状态：62 个已跟踪修改、287 个未跟踪文件；未机械沿用旧报告数量。
- 2026-08-04 候选暂存前审计快照：66 个已跟踪修改、289 个未跟踪文件，合计 355 个工作树变更文件；`git diff --stat` 为 66 files / 18,325 insertions / 1,766 deletions。未跟踪文件按顶层目录为 backend 276、deploy 2、docs 7、frontend 2、scripts 2。
- Git 候选集合为 621 个文件（既有跟踪文件 332 + 未跟踪文件 289），其中 617 个文本文件通过秘密扫描；4 个既有二进制基线文档未被本轮改写。候选提交后的 SHA 与干净状态以最终报告为准。
- 忽略目录中的本机 `.env`、`backend/target`、两个前端 `dist` 和 `node_modules` 均未进入候选；未发现待提交日志、备份、压缩包、数据库转储、私钥、令牌或生产环境文件。

## 2. Flyway 迁移（9 个阶段1向前迁移）

- `backend/src/main/resources/db/migration/V4__stage1_organization_identity.sql`
- `backend/src/main/resources/db/migration/V5__stage1_role_data_scope.sql`
- `backend/src/main/resources/db/migration/V6__stage1_workflow_task_message.sql`
- `backend/src/main/resources/db/migration/V7__stage1_common_governance.sql`
- `backend/src/main/resources/db/migration/V8__stage1_masterdata_integration.sql`
- `backend/src/main/resources/db/migration/V9__seed_stage1_permissions_and_defaults.sql`
- `backend/src/main/resources/db/migration/V10__stage1_security_workflow_integration_remediation.sql`
- `backend/src/main/resources/db/migration/V11__stage1_batch_authorization.sql`
- `backend/src/main/resources/db/migration/V12__stage1_message_action_authorization.sql`

V1–V3 未改写。V6/V7 无历史提交或可核验冻结环境执行证据，但无法反证外部临时库执行，已按保守策略锁定当前 SHA-256；任何修正只能新增迁移，不得 `repair`。

## 3. 后端源码与测试

- `backend/src/main/java/com/srm/masterdata/`：组织、工厂/仓库扩展、主数据、外部映射、数据范围和批处理门面。
- `backend/src/main/java/com/srm/system/`：用户/角色/数据策略、审批/任务/消息、字典/参数/编号/模板/附件/审计、Inbox/Outbox 和批任务。
- `backend/src/main/java/com/srm/security/`、`config/`、`platform/`：主体属性、首次改密、MyBatis-Plus 乐观锁、Clock、附件配置与受控公开契约。
- `backend/src/main/resources/mybatis/masterdata/` 与 `mybatis/system/`：参数化复杂查询；无 MyBatis 二级缓存。
- `backend/src/test/java/com/srm/`：23 个测试源文件，包含数据范围、权限链、审批、SLA、审计、主数据、附件、Flyway 升级和架构边界。

## 4. 前端与 OpenAPI

- `frontend/module-manifest.json` 与 `frontend/apps/internal-web/src/router/route-registry.ts`：12 域、18 启用、69 隐藏的安全路由白名单。
- `frontend/apps/internal-web/src/views/workbench/`、`masterdata/`、`system/`：18 个启用页面的真实列表、分页、查询、详情和主操作闭环。
- `frontend/apps/internal-web/tests/enabled-page-contracts.test.mjs`：页面契约、分页、详情和精确权限回归。
- `docs/04-接口与数据设计/openapi/internal-api.json`：当前后端实际导出的 127 paths / 165 operations / 112 schemas。
- `frontend/packages/shared-types/src/generated/internal-api.ts` 及 `frontend/packages/api-client/src/index.ts`：生成类型、Blob 下载和 FormData 上传能力。

## 5. 工程、部署和运维

- `scripts/check.sh`、`check-migrations.sh`、`generate-skeleton.mjs`、`validate-skeleton.mjs`、`validate-architecture.mjs`：阶段1全量门禁。
- `scripts/validate-security.mjs`：仅扫描 Git 提交候选文本，排除明确忽略的本地 `.env`。
- `scripts/backup.sh` 与 `scripts/restore.sh`：备份、manifest/SHA-256、非空目标拒绝、隔离恢复和恢复后校验。
- `.github/workflows/srm-release.yml`：候选分支门禁、MySQL 8.4迁移校验、三个 `linux/amd64` 业务镜像的短SHA标签、TCR Secrets与digest摘要。
- `deploy/stage1-acceptance.env.example` 与 `deploy/STAGE1_ACCEPTANCE.md`：保留为历史受保护样例和正式部署变更窗口参考；当前已取消临时验收环境，不复制环境文件、不填写临时秘密、不执行其中命令。
- `docker-compose.yml`、`deploy/docker-compose.prod.yml`、`deploy/docker/`、`deploy/nginx/`：五服务、健康检查、MySQL 8.4、附件/数据卷和生产秘密必填约束。

## 6. 交付文档

- `docs/03-模块详细设计/阶段1平台与数据底座详细设计.md`
- `docs/04-接口与数据设计/阶段1系统与主数据表数据字典.md`
- `docs/04-接口与数据设计/阶段1菜单与角色权限矩阵.md`
- `docs/05-测试验收与运维/阶段1开发与自复核报告.md`
- `docs/05-测试验收与运维/阶段1文件清单.md`
- `docs/05-测试验收与运维/阶段1备份恢复演练报告.md`
- `docs/05-测试验收与运维/本地运行手册.md`
- `docs/05-测试验收与运维/构建与恢复说明.md`

## 7. 当前验收结论

- 源码、自动化门禁、GitHub Actions、MySQL 8.4 空库 V1→V12 及三个 `linux/amd64` 业务镜像构建推送已通过，结论为“阶段1源码与CI验收通过”。
- 服务器部署、现有数据库升级、备份恢复和运行验证延后至正式部署时完成；当前未验证、不计为已通过，但不阻断阶段1代码合并和阶段2任务书编制。
