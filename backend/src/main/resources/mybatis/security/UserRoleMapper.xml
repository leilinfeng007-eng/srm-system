<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.srm.security.infrastructure.persistence.mapper.UserRoleMapper">
    <select id="selectActiveRoleId" resultType="java.lang.Long">
        SELECT id
        FROM sys_role
        WHERE role_code = #{roleCode}
          AND status = 'ACTIVE'
    </select>

    <select id="countAssignment" resultType="long">
        SELECT COUNT(*)
        FROM sys_user_role
        WHERE user_id = #{userId}
          AND role_id = #{roleId}
    </select>
</mapper>
