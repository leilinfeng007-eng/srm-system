<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.srm.security.infrastructure.persistence.mapper.TokenSessionMapper">
    <select id="countActiveAccess" resultType="int">
        SELECT COUNT(*)
        FROM sys_refresh_token
        WHERE id = #{sessionId}
          AND user_id = #{userId}
          AND access_jti = #{accessJti}
          AND revoked_at IS NULL
          AND expires_at &gt; #{now}
    </select>
</mapper>
