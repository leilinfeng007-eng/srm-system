<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.srm.security.infrastructure.persistence.mapper.UserAccountMapper">
    <select id="selectActiveRoles" resultType="java.lang.String">
        SELECT r.role_code
        FROM sys_role r
        JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND ur.status = 'ACTIVE'
          AND r.status = 'ACTIVE'
        ORDER BY r.role_code
    </select>

    <select id="selectEnabledPermissions" resultType="java.lang.String">
        SELECT DISTINCT p.permission_code
        FROM sys_permission p
        JOIN sys_role_permission rp ON rp.permission_id = p.id
        JOIN sys_user_role ur ON ur.role_id = rp.role_id
        JOIN sys_role r ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND ur.status = 'ACTIVE'
          AND r.status = 'ACTIVE'
          AND p.enabled = TRUE
        ORDER BY p.permission_code
    </select>

    <update id="updateLogin">
        UPDATE sys_user
        SET last_login_at = #{loginAt},
            updated_by = #{actor},
            updated_at = #{loginAt},
            version = version + 1
        WHERE id = #{userId}
    </update>

    <update id="updatePassword">
        UPDATE sys_user
        SET password_hash = #{passwordHash},
            must_change_password = #{mustChange},
            updated_by = #{actor},
            updated_at = NOW(6),
            version = version + 1
        WHERE id = #{userId}
    </update>
</mapper>
